// src/config.js (ou onde quer que você o tenha colocado)
export const serverConfig = {
    name: "npc-brain-service", // Novo nome!
    version: "0.1.0",
    // Poderíamos adicionar aqui configurações específicas do jogo/NPC no futuro
  };
  
  export const transportConfig = {
    type: "stdio", // Mantendo stdio por enquanto
    // opções...
  };